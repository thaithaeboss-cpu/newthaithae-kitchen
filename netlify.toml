[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# SPA-like fallback for deep links (trailingSlash: true in next.config)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
